# Example environment variables for PDF Parsing Application

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Server Configuration
PORT=4000
NODE_ENV=development

# Optional: Additional MongoDB settings
DB_NAME=pdfParsingDB
DB_CONNECTION_TIMEOUT=10000
DB_MAX_POOL_SIZE=10

# Instructions:
# 1. Copy this file to .env
# 2. Replace the MONGODB_URI with your actual MongoDB connection string
# 3. Update other variables as needed for your environment
# 4. Never commit the .env file to version control